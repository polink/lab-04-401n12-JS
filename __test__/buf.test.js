'use strict';

const base = require('../index.js');
const loop = require('../files/loop.js');

describe('buffer array', () => {
    it('has an array with names', () => {
        expect(array.length).toEqual(2);
    })
/*    prints out the following:
Jimmy
Baughb
Larry
 */
});